module.exports = function(app){
    const etc = require('../controllers/etcController');
    const {verify,upload} = require('../../../config/middlewares');

    //app.put('/user/profile',upload.single('img'),etc.updateProfile);

    
};