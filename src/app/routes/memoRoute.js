module.exports = function(app){
    const memo = require('../controllers/memoController');
    const {verify} = require('../../../config/middlewares');

    //app.post('/room/:roomId/memo',memo.createMemo);
    
};